(()=>{const cfg=window.BDJ_AGENDA_CONFIG||{};const sb=supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);const $=s=>document.querySelector(s);let rows=[];const money=n=>Number(n||0);async function auth(){const {data:{session}}=await sb.auth.getSession();if(session)return show();$('#admin-signin').onclick=async()=>{const {error}=await sb.auth.signInWithPassword({email:$('#admin-email').value.trim(),password:$('#admin-password').value});if(error)$('#admin-message').textContent=error.message;else show()}}async function show(){$('#admin-login').hidden=true;$('#admin-app').hidden=false;$('#admin-signout').onclick=()=>sb.auth.signOut().then(()=>location.reload());await load()}async function load(){const {data:accounts,error}=await sb.from('loyalty_accounts').select('customer_id,points,rewards_available,lifetime_points,updated_at').order('points',{ascending:false});if(error){$('#loyalty-list').textContent='Execute a migração 013 da V21 para ativar a fidelidade.';return}const ids=(accounts||[]).map(x=>x.customer_id);const {data:clients}=ids.length?await sb.from('customer_profiles').select('id,name,phone,email').in('id',ids):{data:[]};const map=Object.fromEntries((clients||[]).map(c=>[c.id,c]));rows=(accounts||[]).map(a=>({...a,client:map[a.customer_id]})).filter(x=>x.client);$('#loyalty-total').textContent=rows.length;$('#loyalty-rewards').textContent=rows.reduce((a,x)=>a+money(x.rewards_available),0);$('#loyalty-near').textContent=rows.filter(x=>x.points>=8).length;render()}function render(){const q=($('#loyalty-search').value||'').toLowerCase();const list=rows.filter(x=>(x.client.name+' '+x.client.phone).toLowerCase().includes(q));$('#loyalty-list').innerHTML=list.length?list.map(x=>`<article class="admin-mini-item"><div><strong>${x.client.name}</strong><small>${x.client.phone}${x.client.email?' · '+x.client.email:''}</small><div class="loyalty-bar"><i style="width:${Math.min(100,x.points*10)}%"></i></div><small>${x.points}/10 cortes · ${x.lifetime_points} pontos no histórico</small></div><div><strong>${x.rewards_available?`🎁 ${x.rewards_available} recompensa(s)`:`Faltam ${10-x.points}`}</strong></div></article>`).join(''):'<p class="field-help">Nenhum cliente encontrado.</p>'}$('#loyalty-search').oninput=render;auth()})();
